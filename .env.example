# Database — SQLite local file (không cần thay đổi cho dev)
DATABASE_URL="file:./prisma/dev.db"

# Anthropic API Key — BẮT BUỘC cho tính năng AI scoring
# Lấy tại: https://console.anthropic.com/settings/keys
# Model sử dụng: claude-haiku-4-5 (~$3-6/tháng cho cá nhân)
ANTHROPIC_API_KEY="sk-ant-..."

# Base URL — chỉ cần khi deploy (không cần cho local dev)
# NEXT_PUBLIC_BASE_URL="https://your-domain.vercel.app"
